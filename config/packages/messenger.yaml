framework:
    messenger:
        transports:
            panier_queue:
                transport: amqp
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
            PanierGetOne:
                transport: amqp
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    exchange:
                        name: order_exchange
                        type: direct
                    queues:
                        PanierGetOne:
                            binding_keys: [PanierGetOne]
                    queue_options:
                        durable: false
        routing:
            'App\Message\PanierGetOne': PanierGetOne


